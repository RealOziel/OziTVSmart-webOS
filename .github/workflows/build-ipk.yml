name: Build OziTV webOS IPK

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install Node and webOS CLI
        run: |
          npm install -g @webosose/ares-cli
          ares --version
      - name: Package IPK
        run: |
          mkdir -p build
          ares-package com.oziel.ozitvsmart -o build
      - name: Upload IPK
        uses: actions/upload-artifact@v4
        with:
          name: OziTVSmart-IPK
          path: build/*.ipk
